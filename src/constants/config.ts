const config = {
  baseUrlApi: 'https://risenvn.space/api/',
  baseUrl: 'http://localhost:3000'
  // imageURL: (url?: string) => (url ? '' + url : ''),
  // percentVAT: 10,
  // commentSymbol: '--rating-'
  // maxSizeUploadAvatar: 1048576
}

export default config
